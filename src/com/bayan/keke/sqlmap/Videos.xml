<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 查询当前的版本号-->
<mapper namespace="videos">
    <select id="getVideosInfo" resultType="map" parameterType="com.bayan.keke.vo.KeVideos">
        SELECT
        topic_img AS topicImg,
        video_url AS videoUrl,
        length AS length,
        title AS title,
        description AS description
        FROM ke_videos
        WHERE org_id = #{orgId}
        <if test="page != null and size != null">
            LIMIT
            #{page},#{size}
        </if>
    </select>

    <select id="getOrgId" resultType="string" parameterType="string">
        SELECT org_id AS orgId
        FROM ke_user WHERE user_id = #{userId}
    </select>
</mapper>